{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "popover",
  "type": "registry:ui",
  "title": "Popover ancorado",
  "description": "Balão com bico preso ao elemento, para avisar mudança de contexto. Sem Radix e sem foco-trap: é aviso, não diálogo.",
  "author": "Rabelo Co. <rafael@rabelo.company>",
  "dependencies": [
    "lucide-react"
  ],
  "registryDependencies": [
    "https://rabelo.company/r/ember-theme.json"
  ],
  "files": [
    {
      "path": "registry/rabelo/popover.tsx",
      "target": "components/ui/popover.tsx",
      "type": "registry:ui",
      "content": "// Popover ancorado, recriado no estilo do site (o do 21st.dev não é acessível\n// sem login e traria Radix para um projeto que não usa Radix).\n//\n// Fica posicionado em relação ao elemento pai, que precisa ser `relative`.\n// Sem foco-trap nem portal de propósito: aqui ele é um aviso, não um diálogo —\n// o visitante continua digitando no input embaixo dele.\n\nimport type { ReactNode } from \"react\";\nimport { X } from \"lucide-react\";\nimport { cn } from \"@/lib/utils\";\n\nexport function Popover({\n  open,\n  title,\n  children,\n  onClose,\n  closeLabel,\n  className,\n}: {\n  open: boolean;\n  title?: ReactNode;\n  children: ReactNode;\n  onClose?: () => void;\n  closeLabel?: string;\n  className?: string;\n}) {\n  return (\n    <div\n      data-open={open}\n      role=\"status\"\n      aria-live=\"polite\"\n      className={cn(\n        \"popover absolute bottom-full left-1/2 z-40 mb-3 w-[min(21rem,calc(100vw-2.5rem))] -translate-x-1/2\",\n        className\n      )}\n    >\n      <div className=\"relative rounded-2xl border border-border-strong bg-surface-2 p-4 shadow-2xl\">\n        {onClose && (\n          <button\n            onClick={onClose}\n            aria-label={closeLabel}\n            className=\"absolute right-2.5 top-2.5 text-faint transition-colors hover:text-fg\"\n          >\n            <X className=\"h-3.5 w-3.5\" />\n          </button>\n        )}\n        {title && <p className=\"pr-6 font-display text-sm font-semibold\">{title}</p>}\n        <div className=\"mt-1.5 text-xs leading-relaxed text-muted\">{children}</div>\n        <span aria-hidden className=\"popover-arrow left-1/2 -ml-[5px]\" />\n      </div>\n    </div>\n  );\n}\n"
    },
    {
      "path": "registry/rabelo/styles/popover.css",
      "target": "styles/rabelo/popover.css",
      "type": "registry:file",
      "content": "/* ============ Popover ancorado ============ */\n.popover {\n  opacity: 0;\n  transform: translateY(6px) scale(0.97);\n  pointer-events: none;\n  transform-origin: bottom center;\n  transition:\n    opacity 0.2s ease-out,\n    transform 0.22s cubic-bezier(0.34, 1.4, 0.64, 1);\n}\n\n.popover[data-open=\"true\"] {\n  opacity: 1;\n  transform: none;\n  pointer-events: auto;\n}\n\n/* Bico do popover: quadrado girado, herdando fundo e borda do card. */\n.popover-arrow {\n  position: absolute;\n  bottom: -5px;\n  width: 10px;\n  height: 10px;\n  transform: rotate(45deg);\n  background: var(--color-surface-2);\n  border-right: 1px solid var(--color-border-strong);\n  border-bottom: 1px solid var(--color-border-strong);\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .popover {\n    transition: opacity 0.15s ease-out;\n    transform: none;\n  }\n}\n"
    }
  ],
  "docs": "Importe o CSS deste item no seu arquivo de estilos global: @import './styles/rabelo/<arquivo>.css';"
}
